This extension will automatically open new Google documents in View mode. Pin the extension to your toolbar 
and click on it to enable or disable the feature. You can also use the Google doc native view mode drop down to manually change modes. 

To install:

- Open chrome://extensions/ in Chrome
- Tick the Developer mode checkbox
- Click the "Load unpacked extension" button and select the `gdocs-readonly-mode` directory
